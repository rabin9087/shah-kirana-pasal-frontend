const ProductsDashboard = () => {
    return (
        <div>
            <h1 className="text-2xl font-bold">Products Dashboard</h1>
            {/* Add product-related charts, data, etc. */}
        </div>
    );
};

export default ProductsDashboard;
