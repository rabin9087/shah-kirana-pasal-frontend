export type createUserParams = {
    fName: string;
    lName: string;
    email: string;
    phone: string;
    password: string;
}

export type signInUserParams = {
    email: string;
    password: string;
}
